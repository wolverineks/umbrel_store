version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: wolverineks-hello-world_server_1
      APP_PORT: 3000

  server:
    image: getumbrel/community-app-store-hello-world:latest
    user: "1000:1000"
    restart: on-failure
    init: true