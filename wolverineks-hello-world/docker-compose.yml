version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: wolverineks-hello-world_server_1
      APP_PORT: 8080

  server:
    build: .
    image: wolverineks-hello-world:local
    restart: on-failure
    init: true